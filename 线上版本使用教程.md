# my-neuro 线上版本 · 使用教程

> 📌 **网页版（推荐）：** 双击 `live-2d/docs/打开教程网页.bat`，或根目录 `打开教程网页.bat`，或打开 [`live-2d/docs/guide-usage.html`](live-2d/docs/guide-usage.html)

> 适用：**线上安装包**（WebUI 标题显示「云端」）  
> 版本：**v6.6**  
> 最后更新：2026-07-03

---

## 这是什么？

**my-neuro（肥牛）** 是一个住在电脑桌面上的 **AI 虚拟伙伴**。

你可以：
- **说话**跟它聊天（它会听、会想、会回答、会出声）
- **打字**跟它聊天
- 看 Live2D 角色做表情、做动作
- 让它记住你说过的重要事情（记忆系统）
- 通过插件扩展更多能力（B 站直播、日记、翻译等）

**线上版本** 和「本地完整版」最大的区别：

| | 线上版本 | 本地完整版 |
|---|---------|-----------|
| 语音合成（TTS） | 走云端（阿里云等），**不用显卡** | 本地 GPT-SoVITS，需要显卡 |
| 安装体积 | 更小 | 更大（含 TTS 模型） |
| 适合谁 | 普通电脑、想省事的人 | 有显卡、想完全离线的人 |

> **⚠️ 已弃用，请勿使用：**「全云端肥牛」（云端配置 → 启动全云端）和 `RAG.bat` / WebUI 里的 RAG 服务均已下线。长期记忆请用 **MemOS 记忆系统**。

---

## 文件夹里都有什么？

解压后，主要关注这些：

```
my-neuro/
├── live-2d/                    ← 核心程序在这里
│   ├── 启动 WebUI 控制面板.bat  ← 【从这里开始】
│   └── go.bat                  ← 启动桌宠（也可在 WebUI 里点按钮）
├── 1.ASR.bat                   ← 语音识别（让你能「说话」）
├── 2.TTS.bat                   ← 本地语音合成（线上版一般用不到）
├── 4.MEMOS-API.bat             ← 记忆系统后端（开桌宠前必启）
├── 5.MEMOS-WebUI.bat           ← 记忆管理网页
├── 3.bert.bat                  ← 情感分析（可选）
└── docs/                       ← 本教程网页
```

**记住一句话：日常用 WebUI 控制面板就够了。** 根目录的 `RAG.bat` 已弃用，请勿运行。

---

## 第一次使用：三步走

### 第 1 步：打开控制面板

1. 进入 `live-2d` 文件夹
2. 双击 **`启动 WebUI 控制面板.bat`**
3. 浏览器会自动打开，看到 **「My Neuro - 控制中心（云端）」**

> 如果浏览器没自动打开，看黑色窗口里写的地址，手动复制到浏览器即可。

---

### 第 2 步：填好 API 密钥

线上版需要配两样东西：**大模型**（负责聊天）和 **阿里云 TTS**（负责说话）。

**① 大模型（负责聊天）**

1. 控制面板 → **LLM 配置**
2. 添加你的 API 提供商（填 API Key、接口地址）
3. 再到 **对话配置** → 选择要用的 **对话模型**
4. 保存

**② 语音（负责说话）**

1. 控制面板 → **云端配置** → **阿里云 TTS**
2. 勾选启用，填入 API Key、模型名、声音 ID
3. 保存

详细步骤见 **[阿里云 TTS 网页教程](docs/guide-aliyun-tts.html)** 或同目录 `阿里云TTS部署教程.md`。

> **注意**：同时只开一种 TTS。启用阿里云 TTS 后，请关闭「字节 TTS」和「云端 TTS（SiliconFlow）」，也不要勾选已弃用的「启动全云端」。

---

### 第 3 步：启动服务，见到肥牛

回到控制面板第一个页签 **「服务控制」**，**按顺序**启动（缺一不可）：

1. 点 **ASR 语音识别 → 启动**，等状态变绿
2. 点 **记忆系统 → 启动**，等状态变绿（**第一次可能较慢**，需等待 Embedding 模型加载完成）
3. 点 **Live2D 主服务 → 启动**

> **重要：** 线上版 **必须先启动记忆系统（MemOS API）**，否则桌宠无法正常跑起来。WebUI 控制面板里若提示「等待 ASR / 记忆系统 端口响应」，说明前置服务还没就绪，请继续等或查看对应日志。

> **关于「一键启动」：** 会依次尝试启动 ASR、TTS、记忆系统等。本地 **TTS、RAG 报错可忽略**；但 **记忆系统必须成功启动**，请留意「记忆系统」状态变绿后再开桌宠。

> **不要启动：** 本地 TTS 语音合成、RAG 服务（均已弃用或线上版不可用）。**记忆管理网页**（`5.MEMOS-WebUI.bat`）按需打开，不影响桌宠运行。

> **线上版不需要启动「TTS 语音合成」**（那是给本地 GPT-SoVITS 用的）。

启动成功后，桌面上会出现 **Live2D 角色**，那就是肥牛。

---

## 怎么跟肥牛聊天？

### 语音聊天（默认）

1. 确保 **ASR 已启动**（或使用百度流式 ASR）
2. 直接 **对着麦克风说话**
3. 肥牛听到后会思考、回答，并 **用语音念出来**

**打断它说话**：按 **`Ctrl + G`**

**按键说话（PTT）**：在 **对话配置** 里勾选「启用 PTT」，默认 **按住 V 键** 说话，松开识别。

**语音打断**：默认已开启。你说话时，肥牛会停下来听你讲。

---

### 打字聊天

1. 控制面板 → **对话配置**
2. 勾选 **「文字输入框（可打字交互）」**
3. 保存，重启桌宠
4. 桌宠界面会出现输入框，打字发送即可

---

### 常用快捷键

| 快捷键 | 作用 |
|--------|------|
| `Ctrl + G` | 打断 AI 说话 |
| `Ctrl + T` | 窗口置顶 |
| `Ctrl + M` | 切换气泡显示 |
| `Ctrl + Q` | 退出程序 |
| 按住 `V` | 按键说话（需先在配置里开启 PTT） |
| `Ctrl + Shift + 1~5` | 触发预设动作 |
| `Ctrl + Shift + 6` | 随机播放音乐 |
| `Ctrl + Shift + 0` | 停止所有动作 |

---

## 控制面板各页签是干什么的？

用大白话解释：

| 页签 | 干什么用 |
|------|----------|
| **服务控制** | 启动/停止桌宠、ASR、记忆等；看日志 |
| **基础配置** | 视觉识图、工具开关 |
| **对话配置** | 选对话模型、开打字框、语音打断、上下文长度 |
| **人格设置** | 改 AI 性格、人设提示词 |
| **LLM 配置** | 管理 API 密钥和模型列表 |
| **云端配置** | 阿里云 TTS、百度 ASR 等（勿用已弃用的全云端肥牛） |
| **Live2D 设置** | 皮套位置、大小、表情动作绑定 |
| **声音克隆** | 本地 TTS 训练（线上版一般用不到） |
| **广场** | 下载提示词、工具、插件资源 |
| **插件管理** | 开关各种扩展功能 |

**改完任何配置，记得点右上角「保存」**，必要时重启桌宠生效。

---

## 记忆系统（启动桌宠的前置条件）

线上版 **必须启动 MemOS 记忆后端**，桌宠才能正常运行。内置记忆插件默认开启，会连接 `http://127.0.0.1:8003`。

### 启动记忆后端（必做）

1. **服务控制** → 点 **记忆系统 → 启动**
2. 等待状态变绿（首次启动加载 Embedding 模型，可能需要 1~3 分钟）
3. 可查看 **记忆日志** 确认是否出现就绪提示

也可手动双击根目录的 `4.MEMOS-API.bat`，效果相同。

### 记忆管理网页（可选）

想查看、添加、搜索记忆时：

1. 确保记忆后端已在运行
2. 点 **打开 WebUI**，或双击 `5.MEMOS-WebUI.bat`

> **顺序：** 必须先有 API 后端（步骤 4），再开管理网页（步骤 5）。管理网页不是开桌宠的必需项。

---

## 插件：给肥牛加技能

线上版已默认启用 **7 个插件**。完整说明见 **[插件使用教程网页](live-2d/docs/guide-plugins.html)**。

在 **插件管理** 页签里可开关和配置。改完保存后，建议 **重启桌宠**。

---

## 常见问题

### 1. 桌宠启动了，但没有声音？

- 检查 **云端配置 → 阿里云 TTS** 是否启用且 Key 正确
- 确认 **字节 TTS、云端 TTS** 没有同时开着
- 看 **服务控制 → TTS 日志 / 桌宠日志** 有没有报错
- 参考 **`阿里云TTS部署教程.md`** 排查

### 2. 我说话它没反应？

- 确认 **ASR 语音识别** 已启动（状态为绿色）
- 检查麦克风是否被其他软件占用
- 系统设置里允许麦克风权限
- 或在云端配置中启用百度流式 ASR 作为替代

### 2.5. Live2D 启动不了 / 提示等待端口？

- 确认 **记忆系统** 和 **ASR** 都已启动且状态变绿
- 首次启动记忆系统较慢，请看 **记忆日志** 是否还在加载
- 记忆后端地址应为 `http://127.0.0.1:8003`，不要被防火墙拦截

### 3. 它能回复文字，但不说话？

- **对话配置** 里 **「启用 TTS」** 不要关掉（这是总开关）
- 云端 TTS（阿里云）要单独配好并启用
- 线上版 **不需要** 启动本地 `2.TTS.bat`

### 4. 一键启动时 TTS / RAG 报错？

线上版 **没有本地 TTS 模型**，一键启动时的 TTS、RAG 报错 **可以忽略**。  
只要 **阿里云 TTS** 配好且 **ASR 已启动**，语音就能正常使用。建议改用手动启动 ASR + Live2D。

### 5. 全云端肥牛 / RAG 还能用吗？

**不能。** 这两项均已弃用：「启动全云端」服务已下线，`RAG.bat` 和 WebUI 里的 RAG 服务也不再维护。请改用 **LLM API + 阿里云 TTS + ASR**；需要长期记忆请用 **MemOS 记忆系统**。

### 6. 改了配置没生效？

1. 点右上角 **保存**
2. **服务控制** → 重启 **Live2D 主服务**
3. 还不行就 **一键停止** 再 **一键启动**

### 7. 对话历史记忆（persistent history）要不要开？

**暂时不要开。** 配置页有提示：目前有 bug 可能导致 AI 复读混乱。

### 8. 电脑配置要求？

| 项目 | 建议 |
|------|------|
| 系统 | Windows 10/11 |
| 内存 | 8 GB 以上（开 ASR 建议 16 GB） |
| 显卡 | 线上版 **不强制**（语音走云端） |
| 网络 | 需要稳定联网（云端 API） |
| 麦克风 | 语音聊天需要 |

---

## 推荐使用流程（一张图记牢）

```
打开 WebUI 控制面板
        ↓
配好 LLM API + 阿里云 TTS
        ↓
启动 ASR 语音识别
        ↓
启动 记忆系统（MemOS，必做）
        ↓
启动 Live2D 主服务
        ↓
对着麦克风说话 / 打字聊天
        ↓
想改性格、皮套、插件 → 控制面板里改 → 保存 → 重启桌宠
```

---

## 进阶与更多资料

| 需求 | 去哪里看 |
|------|----------|
| 默认启用插件说明 | [`live-2d/docs/guide-plugins.html`](live-2d/docs/guide-plugins.html) / [`插件使用教程.md`](插件使用教程.md) |
| 阿里云语音详细配置 | [`live-2d/docs/guide-aliyun-tts.html`](live-2d/docs/guide-aliyun-tts.html) |
| 官方视频/图文教程 | [mynewbot.com/tutorials](http://mynewbot.com/tutorials) |
| 项目 GitHub | [github.com/morettt/my-neuro](https://github.com/morettt/my-neuro) |
| 爱发电赞助 | [ifdian.net/a/xxxiu](https://ifdian.net/a/xxxiu) |

---

## 小结

线上版的核心思路就四句话：

1. **用 WebUI 控制面板管理一切**
2. **聊天靠大模型 API，说话靠云端 TTS（如阿里云）**
3. **先启动 ASR + 记忆系统，再启动桌宠**
4. 遇到问题先看 **服务控制** 里的日志
